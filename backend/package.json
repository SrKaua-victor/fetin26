{
  "name": "bustrack-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon --exec \"node --env-file-if-exists=../.env\" src/index.js",
    "start": "node --env-file-if-exists=../.env src/index.js",
    "check": "node --check src/index.js && node --check src/db.js && node --check src/auth.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
